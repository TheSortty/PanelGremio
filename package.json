{
  "name": "panel-gremio-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Dependencias de desarrollo para el frontend para dar soporte a TypeScript y al editor de código (VSCode).",
  "scripts": {
    "install-deps": "npm install"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.4.5"
  }
}
